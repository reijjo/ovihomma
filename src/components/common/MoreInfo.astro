<div class="tuote-moreInfo wrapper">
  <slot />
</div>

<style>
  .tuote-moreInfo {
    gap: 0.5rem;
    display: flex;
    flex-direction: column;
    width: min(100%, 800px);
    text-wrap: pretty;
  }
</style>
