---
import Button from "@/components/ui/Button.astro";
---

<section class="hero-section">
  <div class="hero-content wrapper">
    <h1 class="hero-header">SUOMALAISTA PORTTIA JA MUUTA</h1>
    <Button class="hero-button">Ota yhteyttä</Button>
    <div class="hero-overlay"></div>
  </div>
</section>

<script>
  function resetHomeScroll() {
    if (window.location.pathname !== "/") return;
    window.scrollTo({ top: 0, left: 0, behavior: "auto" });
  }

  resetHomeScroll();
  document.addEventListener("astro:page-load", resetHomeScroll);
</script>

<!-- Styles -->
<style>
  .hero-section {
    display: grid;
    place-items: center;
    height: 80dvh;
    width: 100%;
    background-image: url("/images/etusivu/heroholder.jpg");
    background-position: center;
    background-size: cover;
    position: relative;
  }

  .hero-content {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    gap: 1rem;
  }

  .hero-header {
    font-size: 3rem;
    text-align: center;
    text-wrap: balance;
    z-index: 2;
    color: white;
    position: relative;
  }

  .hero-button {
    position: relative;
    z-index: 2;
    height: 3rem;
    transform: scale(1.125);
  }

  .hero-overlay {
    width: 100%;
    height: 100%;
    background: linear-gradient(to bottom, rgb(0 0 0 / 10%), rgb(0 0 0 / 40%));
    z-index: 1;
    position: absolute;
    top: 0;
    left: 0;
  }
</style>
